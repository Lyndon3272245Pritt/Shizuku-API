plugins {
    id('com.android.library')
}

android {
    namespace 'rikka.shizuku.aidl'
    buildFeatures {
        buildConfig false
        aidl true
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
    compileOptions {
        // Bumped to Java 17 for better language feature support
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    // Enable lint checks to catch potential issues during development
    lint {
        abortOnError false
        warningsAsErrors false
    }
}

ext {
    publishLibrary = true
    POM_NAME = "Shizuku API - aidl"
    POM_DESCRIPTION = "Low level aidl of Shizuku and Sui."
}
